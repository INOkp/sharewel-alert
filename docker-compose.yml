services:
  sharewell-alert:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    environment:
      SHAREWEL_STATE_FILE: /data/sharewell_state.json
    volumes:
      - ./data:/data
